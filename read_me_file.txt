1) set up new conda environment using requirements.txt
   pip freeze > requirements.txt
   Run the above command in the terminal and update the required packages needed to run housing.ipynb notebook file

2) run  the housing.ipynb  notebook file for Exercises output.
   (already the notebook file is executed and outputs are displayed.